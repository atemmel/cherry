# Perform string interpolation with braces

var name = "James"

say "Hello {name}"

assert (== "Hello {name}" "Hello James")

var age = 24

say "You are {age}"

assert (== "You are {age}" "You are 24")

# Escape interpolations with double braces

say "{{age}}"

assert (== 5 (len "{{age}}"))
